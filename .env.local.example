# Supabase Auth (login del admin) — el admin valida con Supabase Auth
# y luego el backend del bot verifica que el email esté en ADMIN_EMAILS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# URL del bot (donde viven los endpoints reales del admin).
NEXT_PUBLIC_BOT_API_URL=https://freshco-bot-l75x.vercel.app
